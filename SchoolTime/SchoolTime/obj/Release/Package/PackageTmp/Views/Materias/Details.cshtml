@model SchoolTime.Models.Materia

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Materia</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayFor(model => model.Nombre)
        </dt>
        <dt>
            
        </dt>
        <dd>
            <table class="table">
                <tr>
                    <th>Tarea</th>
                    <th>Detalle</th>
                    <th>Fecha de entrega</th>
                    <th>Punteo</th>
                </tr>
                @foreach (var item in Model.AsigancionTarea)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Tarea.Nombre)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Tarea.Detalle)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Tarea.FechaEntrega)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Tarea.Punteo)
                        </td>
                    </tr>
                }
            </table>
        </dd>
    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
